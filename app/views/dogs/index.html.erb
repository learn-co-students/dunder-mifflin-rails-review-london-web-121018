<h1>All Dogs</h1>

<%@dogs.each do |dog|%>
<p><%= link_to dog.name, dog_path(dog) %> <%=dog.employees.count%></p>


<%end%>
<%=button_to("sort", dogs_sort_path, html_options ={method:"post", params: {name:"sort"}})%>
